import React from 'react';
import styles from './Portfolio.module.scss';

const Portfolio = () => {
  return <div className={`${styles.portfolio} container`}>Portfolio</div>;
};

export default Portfolio;
